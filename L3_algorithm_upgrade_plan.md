# L3 算法升级方案：边界激活合并与增量链任务

## 结论与范围

本方案依据 GitHub MCP 对以下版本的核查，不是已完成的 GPU 优化：

- 30h 结果提交：`f1af591d1c6323d69df4878f3da2c93e6f0746a5`；受测源码 `9dd69fa02777853537e8068e90996b0ee7cc4186`。
- 动态链结果提交：`151060391fbab5bb42d6f13911d3af1e83234066`；受测源码 `a91a61e5c8949b29dccaea1e8d8abc242a71a7de`。
- 本轮没有执行 GPU 程序，没有修改或推送服务器代码。附带的 Python 仅是顺序数学模型。

建议保留 30h 的同步、容量和计时基线，不再直接开启旧 `L3_CHAIN_PARTITION`。下一项算法实验应先回答：远端更新引起的哪些激活可以在执行前合并，哪些链传播已经被完成的工作支配？然后才决定是否实施“版本化链任务 + 增量传播”。

目标不是证明其他算法永远不能使用链技术，而是证明：在相同逻辑图、合理优化单卡及一致计时边界下，L3 对远端更新的处理方式带来可归因的额外收益，并达到 `T1/T2 >= 1.20`。通用链运算本身必须有有效的单卡控制。

## 1. 已知事实：两条实验线证明了什么

### 1.1 30h 的正结果和限制 [R1–R3]

USA G+ 的主测与独立复测均使用 `delta=400000`：

| 批次 | 单卡 T1 (ms) | 双卡 T2 (ms) | T1/T2 |
|---|---:|---:|---:|
| 38304 主测 | 73.3605045 | 64.6717355 | 1.1343519 |
| 38305 独立确认 | 73.3405565 | 64.5799375 | 1.1356554 |

同一源码的八图结果使用 `delta=200000`，USA G+ 是 1.0844039，八图几何平均 G/G+ 为 0.8881844/0.9298584。不能把 1.136 替换进这组八图图表。两个附加 USA 源点分别是 0.7343 和 1.0908，说明固定源点的正收益不能外推为源点无关的扩展性。

退避、blocks、短窗口、BFS/layer-split 与 compact-candidate 已测试；没有形成 1.20 的正式结果。NVSHMEM 仅是在当时限定搜索路径内未发现可用依赖，未完整评测，不能称为算法上已经失败。

### 1.2 旧动态链的负结果 [R4]

采用原始 G、相同源点及匹配 B/C 执行配置，solve-only 中位数：

| 图 | 单卡 A | 双卡无链 B | 双卡动态链 C | B/C | A/C |
|---|---:|---:|---:|---:|---:|
| USA | 96.118 | 97.897 | 116.345 | 0.8414 | 0.8261 |
| COL | 8.660 | 10.132 | 10.707 | 0.9463 | 0.8088 |

query-wall 中 USA A/C=1.4046 是另一计时边界，不是 solve-only 达标，更不是 chain 的增量收益。B/C 同样为负。

USA/COL 链内部顶点覆盖约 17.72%/19.19%，平均每条链只有 2.79/2.91 个内部顶点。重复访问同一 segment 的 closure 比例约 93.10%/93.30%；B/C 寄存器分别为 76/85。索引构建、上传和额外显存又是新增成本。正确性通过说明原型不是完全不触发，但不说明开销必然能够摊销。

### 1.3 两个不能直接作出的推论

**重复访问不等于无效访问。** `repeated_closures` 只按 segment 是否访问过计数。相同链可能收到更小距离、不同位置或方向的新改进。不能写 `visited[segment]` 后只处理一次，也不能声称已经找到“可删除的 93% 工作”。

**更新数比例不等于耗时比例。** 30h 的 W320 诊断中约 956.5M 本地边展开与 203,735 次跨区更新，只能提示检查本地重复工作；它不是 W512 的时间分解。少量迟到消息仍可能导致大量本地重算，或处于求解关键路径。寄存器增量也只是成本线索，不是单独完成的因果消融。

## 2. 到 1.20 还有多远：必须区分 G 与 G+

用独立正式复测估计，同 G+ 下：

```
目标 T2 <= 73.3405565 / 1.20 = 61.1171304 ms
现有 T2 = 64.5799375 ms
至少还需减少 3.4628071 ms，即 5.3621%
```

这条路线允许双方共同使用相同 G+；新增算法的收益仍可归因于 L3 的运行时处理，而不是把 shortcuts 占为独有。

若坚持在原始 G 上、不依赖双方的静态增强，根据链实验的单卡 A=96.118 ms：

```
目标 T2 <= 80.0983333 ms
从无链 B=97.897 ms 出发，需要约 18.18% 净降时
从旧链 C=116.345 ms 出发，需要约 31.15% 净降时
```

这些只是基于历史正式数据估计的预算，不是新实验的固定分母。新实验重新采集合理优化的 T1。如果共同算法也使 T1 更快，目标 T2 也必须相应降低。

## 3. 第一候选：接收侧版本化边界激活合并

### 3.1 它与当前实现的差异 [R5]

发送端已经按远端目标做 candidate minimum 合并，再做批量发送。不能重新命名现有 bitmap 就当新优化。

当前 `l3_receive_to_l2()` 对每个成功的 authoritative-distance atomicMin 生成一个 L2 winner，并在提交之后 ACK。建议验证的是：**多个先后到达、尚未完成传播的同一 owner 顶点改进，能否共用一个可持续更新的工作责任，而非每次新增激活。**

普通 worker 已有旧任务过滤，所以减少 token 数并不自动意味着减少展开边数。必须分别测量队列访问节省、首次实际展开前的合并比例、已经展开后的新改进比例和总边展开量。

### 3.2 最小状态与安全交接

仅对远端更新可能触达的边界顶点维护稀疏状态：当前最小候选、版本、`IDLE/QUEUED/ACTIVE` 状态。每个顶点至多有一个待消费 token；ACTIVE 期间的进一步下降留在 pending 状态并要求重新执行。

建议协议的逻辑要求如下，原子指令实现仍需单独设计和验证：

1. RX 将新改进并入 owner 状态；没有责任时建立 durable token。已经 QUEUED 时更新其值/版本，不再追加等价 token。
2. worker 领取时读取最新版本和最小值，处理一个确定快照。处理期间到达的更新不得被领取者清空。
3. 提交传播后，若版本改变或仍有更小 pending，则保留或重建责任；只有确认没有新更新才能退回 IDLE。
4. ACK 表示接收方已取得可持续追踪的工作责任，而不是整个顶点传播已经完成。第一版最好保持现有 L2 提交后 ACK 的合同；引入独立 pending 表时必须同时扩展终止检测。
5. 新建责任失败时保留 inbox/journal 或走普通安全队列，不能为了返回 ACK 丢工作。新状态必须参与容量与恢复检查。

所有清空、重挂和版本检查必须形成无遗漏交接，不可以“读取空 -> 普通 store 清零 -> 返回空闲”的竞争实现。GPU 普通距离的下降与调度责任建立之间的时间窗口，也要由现有活跃角色或明确计数覆盖。

### 3.3 调度感知只作为第二个独立消融

若诊断显示远端更新明显落后于本地 bucket 进度，并触发重复展开，再测试有上限的接收聚合/优先注入。利用现有 `L3_RX_LAG_DIAG` 分类到达位置、此前是否展开及重复改善。不同优先级的更新不能被一个 FIFO 大批次长期阻塞。

可以合并尚未处理的更新，但不能无限等待“最终值”。到达期限、本地无工作、对端等待或完成检测时，必须推进待处理项。提示只决定调度，不得成为丢弃更新或宣告全局完成的证据。

仓库已有 `l3_admission_budget.cuh` 的 peer-horizon 原型，而且与当前 worker recovery 存在编译限制。不能直接开启它并宣称实现了新算法；首先核对旧试验，再与本方案的接收侧版本责任区别开。

## 4. 动态链 2.0：把调度单位从一个顶点改成一个版本化链任务

### 4.1 为什么不是再调一次旧 chain

旧路径让带 RX 来源标记的任务在普通 worker 内命中 route 后，展开整条链；来源标记继续沿本地后继传播。改为以下结构才是不同的算法尝试：

```
多个远端派生改进
    -> 同一 segment 的 pending 最小值集合
    -> 一个版本化 segment token
    -> 合并后进行一次链内传播
    -> 提交所有必要距离和端点激活
    -> 发布 completed 证书；新版本继续排队
```

不默认继承无界传播的 RX 标记。普通顶点继续用原 worker；链工作在独立、批量的执行路径中处理，以避免把全链索引/快照变量加入所有普通线程的热路径。独立执行路径的启动、占用和服务资源仍计入 solve time，不能假设这些资源免费。单纯加 `__noinline__` 也不能保证寄存器一定下降，必须检查编译资源与实测。

### 4.2 对任意链内入口精确合并

设严格双向链为 `v[0] ... v[m]`。从 `v[i]` 到 `v[i+1]` 的权重是 `f[i]`，反向权重是 `r[i]`，两方向不要求相等。所有权重非负。

定义：

```
P[j] = f[0] + ... + f[j-1]
Q[j] = r[0] + ... + r[j-1]
b[i] = 本批次在 v[i] 处收到的最小候选距离；无候选时为 INF
```

该批次对链的精确传播为：

```
D_plus[j]  = P[j] + min over i<=j of (b[i] - P[i])
D_minus[j] = -Q[j] + min over i>=j of (b[i] + Q[i])
D[j]       = min(D_plus[j], D_minus[j])
```

实现也可使用一次正向、一次反向线性 sweep；GPU 长链可采用前缀/后缀最小值。已有数据中绝大多数链很短，第一版应优先“一 lane 处理一个短链、warp 批量处理多条链”，而不是一条短链占满一个 warp；仅对长链使用协作扫描。执行粒度仍需实测，不能用理论占用代替时间。中间减法和求和使用有符号宽整数，不能对 INF 直接做无条件加减；满足当前 32 位输出契约的值才写回。

若同一批积累了 k 个有效入口，先合并再 closure 的工作约为 `O(k+m)`，而不是 k 次独立 `O(m)`。这只是单批的复杂度比较：运行中可能不断产生新下降，不能宣称全查询每条链仅执行一次，也不能把 k 的理论节省直接当作性能提升。

### 4.3 用“已完成传播”而不是 visited 位过滤

对 segment 维护已提交的距离包络 `E[j]`。它必须来自已经完成的链展开，且对应端点激活已持久提交。对新入口 `(i,x)`：

```
x >= E[i]  -> 可证明该入口的整条链贡献被已完成传播支配
x <  E[i]  -> 不能丢弃；必须并入 pending 或立即推进
```

理由是包络已经对链闭合。任一目标 j 都满足：

```
E[j] <= E[i] + dist_chain(i,j) <= x + dist_chain(i,j)
```

因此该入口不能进一步降低任何链顶点，也没有尚未提交的端点责任。这与“相同 segment 以前出现过”完全不同。

**不能把普通 `node_data[i]` 直接当 E[i]。** 另一个线程可能刚更新距离，却还没有把对应传播提交。只观察更小的距离就抑制所有通知，会产生遗漏。当前 `last_processed` 的写入时点也必须核对，不能未经证明借用为全链已完成证书。

第一版可以完整线性计算合并后的链；证明正确且出现收益后，再加入区间化传播。沿某一方向，若路径候选已不优于已完成包络，则后续同方向目标也被支配，可停止该方向。不同入口的合并可以产生多个变化区间，不得假定变化区间总是一个连续段。

### 4.4 版本与终止：不可省略的状态

第一版对同一 segment 只允许一个活动执行者。它领取 pending 的快照，而新到达放入下一版本。必须保持以下不变量：

- `completed_epoch` 只在全部必要距离更新和端点任务提交之后发布。
- 处理中的新改进不得被提交者清空；结束时发现版本变化则重挂 token。
- queued、active、pending、保留输出和尾部物化均属于未完成工作。
- ACK 可以在 owner 已持有 durable 责任后返回，但不能因此从全局完成检测中删除该责任。
- 最终逐顶点数组完整物化在 solve 结束前完成；不能放到计时外 D2H 验证阶段。
- 索引外、分支、非严格双向关系、环、溢出和不支持入口回退原始 CSR。队列满和 scratch 分配失败也有不丢任务的回退。

本文件没有把这组顺序规范伪装成已经完成的 CUDA 内存模型证明；实现后需要针对 claim/offer/commit/rearm 的交错测试及 device/system scope 核对。

### 4.5 “只保留两个端点”仅在特殊情况下成立

若链内部没有其他入边、没有内部源点，并且全部入口都被约束到端点，则可用两端距离及路径前缀表示内部距离，在退出前恢复所有内部点。这个条件下的端点中继很便宜。

但当前 runtime builder 不接收 source 参数，现有运行时可以从内部顶点进入。内部存在源点时，两个端点的距离不能恢复内部真值。例如 5 个顶点的单位权链、中间点为源，两个端点距离为 2；仅从端点恢复会把中间点算成 4，而不是 0。必须把内部源设为 anchor 或保留内部入口。

而且，端点压缩/延迟物化本身是通用的图算法优化。即便集成进 L3，它也不能凭“只在双卡开启”获得独占归因。要检验 L3 额外收益，单卡应得到同等链运算能力。

## 5. 先做什么，以及何时停止

不再先跑八图大矩阵。保留 USA 和预选的 COL 原始 G，对照同配置 B/C；在 G+ 上则只测试不依赖严格链拓扑的接收激活策略。G+ 的 shortcut 边可能破坏 degree=2 条件，不得直接将原始图的链索引套到增强 CSR 后抑制新增边的传播。

### 阶段一：可避免工作的上界与实际分布

只用不参与性能结论的诊断构建，分类：

- 同一顶点在首次展开前收到的多次改进，及已有过滤真正省下的工作。
- 同一 segment 相对于已完成包络被支配的入口；pending 中被更新值替代的入口；确实需要进一步传播的下降。
- 每批可合并入口数量、变化区间长度、尾部激活数量、链长度分布、重复的本地边展开。
- 远端更新到首次有效展开的延迟分布，并与本地 bucket 进度关联。

诊断计数以实际可判定事件定义，不能按事后最终距离剔除在线算法当时必须做的工作。离线参考解只用于输出核对或标明为诊断下界，不能成为运行时先验。

若绝大多数重复访问都携带必要的新下降，单纯 memoization 应停止；若同一队列 token 存续期几乎没有可合并更新，接收 coalescer 也应停止，而不是不断加等待时间制造合并率。

### 阶段二：两个最小消融，逐一执行

先实施纯接收侧 pending/version 合并，不改变图、分区或局部队列策略。确认净收益后，再比较 chain worker（关闭、旧整链、合并 closure）。旧整链只用作机制控制，最终性能要与 30h 的无链最佳版本比较。

三个问题分别统计：是否减少总工作，是否降低完整 solve time，是否相对合理单卡达到 1.20。减少工作但变慢属于负性能结果，不应通过只报告 counter 掩盖。

### 阶段三：独立确认

原计划的 reverse-order、十样本中位数、独立 allocation、预定附加源点、逐顶点 oracle、数值范围、ACK/发布/重试/reset/容量检查保持不变。只达到一张图一个源点就准确报告，不能通过修改目标含义宣布普遍成功。

## 6. 归因与验收矩阵

通用链 primitive 生效时，至少在目标图上提供：

| 配置 | GPU | 通用链能力 | L3 接收合并/进度调度 |
|---|---:|---|---|
| S0 | 1 | 无 | 不适用 |
| S1 | 1 | 有，真实处理本地激活 | 不适用 |
| D0 | 2 | 与待比较版本对应 | 关闭新机制、原 L3 |
| D1 | 2 | 与 S1 对应 | 开启新机制 |

若需要隔离通用链与接收调度，D0/D1 应都具有相同通用链能力，另保留双卡完全无链控制。不让一次消融同时更改 term-only worker、寄存器限制、窗口、图布局和同步协议。

定义：

```
T1_best = 已测试合理单卡版本/局部参数中最佳的独立确认时间
S_total = T1_best / T2_new
A_L3   = T2_matched_control / T2_new
```

正式目标是 `S_total >= 1.20`，同时新机制的 `A_L3 > 1` 在独立重复中稳定成立。单卡同等链 primitive 的快数必须计入 T1_best，不能留在附录却继续用未优化单卡作分母。

可补充交互指标：

```
I = (T2_before/T2_after) / (T1_before/T1_after)
```

`I>1` 可以支持“两卡在该机制下获益更多”，但单独不能证明技术只能用于 L3，也不能替代排除配置和样本混杂的消融。工程贡献可以是与 L3 的正确性和调度合同结合后的有效机制，不必宣称数学运算不可移植。

## 7. 与现有工作和原型的边界

Atos 的 SC22 论文已讨论 GPU 内通信、单边操作和动态通信聚合 [P1]。因此，“使用合并”“GPU 自己发消息”本身不是足够的新颖性主张。本方案待验证的特定贡献是远端改进的接收方责任合并、已完成传播证书与 segment 级增量执行如何共同降低 SSSP 的工作放大。

现有 `l3_admission_budget.cuh`、`l3_owner_commit.cuh` 和 producer gate 都有实验用途或兼容性限制 [R6]；生产路径没有因此自动拥有本方案。先查旧阶段记录，再实现差异，避免再次复现已被否决的同一方案。

论文在本轮实测完成前仍只写已验证的 L3，不加入这些拟议算法或预测倍数。

## 8. 附带顺序数学模型

`test_chain_envelope.py` 已执行：1,000 条随机有向双向链，100,000 次候选到达；检查两遍 sweep、前缀/后缀公式与逐入口路径枚举一致，已完成包络的支配过滤，以及 claim 后新到达不被旧 commit 清空。另包含内部源点和普通距离不等于完成证书的反例。

结果位于 `chain_envelope_model_result.json`。它只验证顺序数学模型；没有证明 CUDA 的无锁实现、跨设备内存顺序、全局终止、队列容量或性能。合成模型的过滤比例不代表 USA/COL 的实际比例，更不是新方案已达 1.20 的证据。

## 9. 交付建议

下一轮 Agent 先提交可避免工作诊断及机制决策，再提交最小实现和同配置消融。报告明确区分 G/G+、主/附加源点、solve/query-wall 和是否存在单卡通用链对照。保留已失败的旧链数据，不把新分支名称当成新颖性证据。新代码不默认替换论文版本，只有完成全部验收后再考虑合并。

## 来源

[R1] [30h 完整报告](https://github.com/hideoze/mlmq_tpds_withl3/blob/f1af591d1c6323d69df4878f3da2c93e6f0746a5/L3_30h_performance_report.md)

[R2] [八图原始配置](https://github.com/hideoze/mlmq_tpds_withl3/blob/f1af591d1c6323d69df4878f3da2c93e6f0746a5/evidence/l3_30h_20260923/formal/9dd69fa02777853537e8068e90996b0ee7cc4186/jobA/05_eight_graph/run_manifest.json)

[R3] [同步合同](https://github.com/hideoze/mlmq_tpds_withl3/blob/f1af591d1c6323d69df4878f3da2c93e6f0746a5/evidence/l3_30h_20260923/final/sync_contract.md)

[R4] [动态链报告](https://github.com/hideoze/mlmq_tpds_withl3/blob/151060391fbab5bb42d6f13911d3af1e83234066/L3_chain_runtime_trial.md)；[严格链索引](https://github.com/hideoze/mlmq_tpds_withl3/blob/151060391fbab5bb42d6f13911d3af1e83234066/SSSP/l3/l3_chain_partition.h)；[运行时 target](https://github.com/hideoze/mlmq_tpds_withl3/blob/151060391fbab5bb42d6f13911d3af1e83234066/SSSP/l3/l3_chain_partition.cuh)

[R5] [接收与 L2 提交](https://github.com/hideoze/mlmq_tpds_withl3/blob/f1af591d1c6323d69df4878f3da2c93e6f0746a5/SSSP/l3/l3_receive.cuh)

[R6] [已有 admission budget](https://github.com/hideoze/mlmq_tpds_withl3/blob/f1af591d1c6323d69df4878f3da2c93e6f0746a5/SSSP/l3/l3_admission_budget.cuh)；[已有 owner-commit](https://github.com/hideoze/mlmq_tpds_withl3/blob/f1af591d1c6323d69df4878f3da2c93e6f0746a5/SSSP/l3/l3_owner_commit.cuh)

[P1] [SC22 / Scalable Irregular Parallelism with GPUs: Getting CPUs Out of the Way](https://sc22.supercomputing.org/proceedings/tech_paper/tech_paper_pages/pap515.html)。这里只引用其公开摘要中的设计原则，不把其 BFS/PageRank 结果当作本项目 SSSP 的直接基线。
