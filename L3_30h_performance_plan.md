# L3 30 小时任务书：同图双 GPU / 单 GPU 加速比达到 1.2x

## 1. 目标、版本与不可改变的口径

本任务替代此前 24 小时性能计划的执行顺序；`L3_agent_minimal_tasks.md` 中的同步、验证和复现要求仍有效。L3 保持为论文中的小扩展，不开展新系统架构工程。

依据仓库 `hideoze/mlmq_tpds_withl3@9dd132dac5028de1fd29bb9b6be0b07d83005ffc`：最新提交只新增 A100 数据清单，运行时代码仍是 `07df3ad` 回退同步补丁后的版本。开工时先记录新的 HEAD、工作区 diff 和实际构建命令；不要覆盖服务器未提交工作。

**验收公式不变：S = median(T1) / median(T2) >= 1.20。** T1 是独立无 L3 单 GPU MLMQ，T2 是同一查询由两张 GPU 分区协作的 MLMQ+L3。必须同图、同逻辑源点、同权重、同增强边和同 solve 计时边界。不是相对 ADDS，不是旧双卡/新双卡，不是 G/G+ 交叉比较，也不是两卡分别解不同源点的吞吐率。

逐图、逐固定源点验收，争取 USA 和至少一张非路网达标；只达标一张就明确只是一张，不把它称为普遍扩展性。三张不同源点的稳健性检查另行报告，不用不断换源点获得一次有利结果。1.2x 表示求解速度提高 20%，不是时间减少 20%。

30 小时按获得双 A100 资源后开始的执行窗口安排，含数据检查、编译、探索与验证。另记请求时间和排队时间；排队不受任务书保证。本文件是待执行计划，不是已经开展或完成的 GPU 实验。

## 2. 先固定最新结果，禁止混批

| 证据 | 版本/作业 | 已公开且可使用的结论 |
|---|---|---|
| 原六配置结果 | 37327 + ADDS 37646 | 同 G+ 的 ADDS/双卡 1.7035x；单卡/双卡 GM 0.9494x；USA 1.1047x |
| 同步补丁版 | 38059；定向检查 38075 | 同 G+ 单卡/双卡 GM 0.921x；USA 1.068x；不能作为当前回退版结果 |
| 最新回退版复测 | 38082 | 384 查询全部校验有效；同 G+ 单卡/双卡 GM 0.959x；USA 75.324 / 69.103 ms = 1.090x |

USA 旧观测对应的目标是 T2 <= 62.770 ms，约需再省 6.333 ms（9.16%）。这仅估计难度；最终必须用新同批 T1/1.2 更新门槛，不能沿用有利的历史分母。报告中的 USA 两轮比值约 1.095x / 1.059x 也应保留，不以一次最好样本定胜负。

**第 0 阶段同时补齐论文证据：**从服务器 `tmp/l3_no_sync_rerun_20260923/` 定位 38082 的真实 records/manifest、原始日志及逐图表，导出紧凑汇总并推送。当前 GitHub 报告只公开该批 GM 和 USA 摘要，不能据此重建其八图六条完整曲线。文件路径以服务器实际发现为准，不假定存在某个未核对的文件名。

回退版的查询通过不是跨设备同步证明。最终候选必须在拟提交版本的明确同步合同下验证和计时；不得靠移除同步、ACK、恢复或终止检查跨过 1.2x。修补共享 `core/` 时，单卡同步采用同一修复。

## 3. A100 数据清单：先验证小集合，不扫描 2,811 张图

数据根目录：`/a100-data1/Dingzhong/mlmq/data`。`A100_integer_dataset_inventory.md` 记录根目录 2,714 项、`bfs-sssp-int/` 21 项、`sssp-int/` 76 项，合计 2,811 个文件条目/136.80 GiB，未按内容去重。清单只读取 header 和大小；4-byte edge 不能证明 int32，更不证明全部边权非负、无溢出或已通过 GPU SSSP。

以下是预先固定的候选顺序，不是已经有效的性能结论。所有相对路径已出现在清单中。

| 优先级 | 路径/输入 | 本轮用途 |
|---|---|---|
| P0 | 现有 manifest 对应 USA 的冻结 G+；原始数据另有 `sssp-int/USA-road-d.USA.gr` | 主目标与版本回归；原始同名文件不能代替冻结重排/增强输入 |
| P0 | `sssp-int/delaunay_n23.gr` | 第一张新增几何图；小型 smoke 用 `sssp-int/delaunay_n20.gr` |
| P0 | `rgg_n_2_20_s0.gr` | 第二类几何图，先快速检查权重/可扩展性 |
| P0 | `nlpkkt80.gr` | 工程图候选；须先核实转换后的权重语义 |
| P0 | `sssp-int/atmosmodm.gr` | 另一工程图候选，不能从文件名假定运行结果 |
| 对照 | `sssp-int/rmat22.gr` | 检查高跨区更新压力；不作为必然会加速的输入 |
| P1 | `sssp-int/delaunay_n24.gr` | 仅当 n23 值得继续且内存/队列允许时扩规模 |
| P1 | `rgg_n_2_24_s0.gr` | 清单记录 16,777,216 顶点、265,114,400 条有向边；有证据再扩规模 |
| 备用 | `circuit5M.gr`、`sssp-int/GAP-road.gr` | 第一组不适用时最多补两张，不做无界择优筛选 |

不优先处理 sk-2005、it-2004、webbase-2001 等超大 Web 图，不把同图 `_1` 或不同目录副本算成独立数据集，不重新下载已有图。初筛最多上述六个 P0/对照输入，第二阶段最多加入两个 P1/备用；所有淘汰和不达标结果保留。

### 数据准入检查

- 记录 realpath、SHA256、header、实际长度、行偏移、目标范围、顶点/边数和索引位宽；检查 padding、截断及分配字节数溢出。排除空文件、backup 文本与隐藏临时文件。
- 对入选文件全量流式检查边权并核对转换来源：int32/float32 不能只靠 header 判断；记录负权、零权、最大权、权重分布。来源不明不能静默按整数冒用。无权图采用显式单位权时须单列为这种输入；不能改权重寻找有利速度。
- 生成原图的 int64 Dijkstra oracle，并记录不可达点。对 GPU 32 位路径检查每次候选加法的安全性，不能仅核对最终数组掩盖中间溢出。粗略 `(n-1)*max_weight` 是充分条件而不是必要条件；不满足它时用宽整数诊断/正确的受检加法核实，不直接把图判为非法。
- 单卡和双卡都必须装入目标显存；统计 CSR、候选、inbox、L2、索引和工作区的总占用。80 GB 显存不等于现有 int 索引、队列计数和每桶容量无限。新增图必须通过现有容量诊断。
- 新图先用 G，不自动增加 shortcuts。预先固定原编号源点（例如有出边顶点序列的 0、1/3、2/3 分位点，记录精确 ID），布局后只做同一源点映射。开发只使用第一个源点；其余用于独立确认。若只能到达很小分量，如实披露，不拿几乎无工作查询证明扩展性。
- delta=200000 是既有路网配置，不是新图通用配置。根据实际权重尺度确定少量候选；单/双卡拥有相同本地队列与参数调优机会。不要以两卡专用的错误队列布局拖慢单卡。

## 4. 三种方向的取舍与源码落点

### A. 优先：减少空转和更新等待，复用已有开关

源码：`SSSP/sssp_run.cu`、`SSSP/sssp.cuh`、`SSSP/l3/l3_window.h`。

1. 在最有希望的两个输入上测试 `MLMQ_WORK_BLOCKS=107/80/64` 与 `L3_IDLE_BACKOFF=false/true`，保持 W512、图和协议不变。每个阶段最多保留两个候选。单卡也测试相同 block 候选。
2. 现有退避表达式请求 64/128/256 ns，不能按注释把它当成 2 us。若分配不对称有诊断证据，再新增逐卡 blocks 参数，只试 107/64 和 64/107；同步调整 manager 使用的真实 worker/ACK 数。
3. 当前 boundary-index 配置的窗口 min=max=25000。先统一参数化初值、probe、cooldown、clamp 和合法性检查，再比较固定 25000/12500/6250 cycles；只有短窗口有收益才试 min=6250/max=25000。不能只改最大值，也不能同时随机打开多个控制器。
4. 保留 retained TX、权威扫描、反馈排空、generation/ACK、RX 提交和完整终止；4096 是 TX 容量，不是必须等满的发送阈值。

**入口陷阱：**`run_supplement_matrix.py` 清理外层环境并重新写入 blocks=107、delta 等；`build_l3_paper_config.sh` 的宏写在 cmd 数组，不能靠外层任意 CXXFLAGS 自动生效。新增显式筛选入口并归档实际命令，保留正式 runner 原样。README 的 cut 摘要遗漏 USA，实际 runner 中 USA=60%。

### B. 主线：在加载/转换阶段生成适合多卡的布局，而不只是换文件后缀

源码：`SSSP/csr_graph.cu::readFromGR` 已读取 CSR 数组；`SSSP/graph_partition.h` 按连续顶点区间截行；`relax_dst` 在热路径判断 owner。只把同一数组预存为 .csr 主要省 setup，不会自动改善当前 solve-only 比值。

**B1，先不改运行时结构。**比较当前布局与 `graph_transform/reorder_gr.cu` 的 BFS/`--layer-split` 中一个有针对性的替代布局。该工具已有 perm/invperm 输出，不能把它的存在当作已验证性能。任意二分得到 owner 后，可通过重编号把 owner0、owner1 分别排成连续区间，继续复用现有运行时。第一版不引入任意 owner 映射或多卡迁移协议。

分区目标同时考虑两侧实际工作、第一批有效 RX 到达、跨卡不同目标数及发送量，不只最小化 cut：一条长链中间切开虽只有一个割边，仍可能近乎串行。BFS 层不等于有权最短距离层，layer-split 可能增加通信，只作为对照。禁止用 oracle 距离做种子/运行时预知信息。

**B2，B1不足且诊断支持时只选一个小改动。**

| 拟议结构 | 预期作用与限制 |
|---|---|
| 内部顶点标记/每行 local 与 remote 分段 | 内部顶点走纯本地松弛，边界顶点分路；不重写 L0-L2。稳定分段后的规范 CSR 同样给单卡，记录邻接顺序变化。 |
| 远端边预编码 compact slot，TX 使用反向映射 | 避免每条跨边 dense-to-compact 查表；保持接收 wire ID、owner 距离和完整候选域一致。先证明收益再缩分配。 |

`SSSP/l3/l3_boundary_index.cuh` 已有 `L3_COMPACT_CANDIDATES` 原型，但候选仍按旧大小分配，不能称为已经压缩全部存储；真正缩分配前必须检查所有 fullscan、reset、requeue、retained、recovery 和终止入口。发送候选边界域不等于 owner 的全部未完成工作域，不能把最终 owner 恢复扫描简单缩成边界。

边界索引从 device 回读构建移到 host，可改善 setup；只有布局/访问域改善才能算 solve 加速。每次变更要记录 setup_ms、额外内存、实际发送记录和净 solve_ms。

cut 改变时，G+ 的 owner-local shortcuts 必须按新 cut 重建并让单/双卡读取同一份。不能旧单卡 G+ 对新双卡 G+，也不能把既有 shortcuts 任意重新切分后仍宣称它们全是 owner-local。

### C. 有条件：NVSHMEM put-with-signal，限时试验而非默认迁移

当前 `SSSP/l3/l3_bulk.cuh::bulk_publish_l3_batch` 已由 GPU warp 直接写 P2P payload，再发布 count/epoch/READY；没有逐批 CPU 中转可以直接消除。现有 host 是一个进程管理两张 GPU；NVSHMEM 常规模型是每 PE 一个 GPU，并有对称内存、初始化和启动约束。因此不是单函数无成本替换。[N1][N2]

先查服务器已安装版本、bootstrap/MPI、CUDA 12.4 和 A100 兼容性。只有依赖已具备且诊断说明发布/通知的关键路径占比足够大，才投入最多 4 小时；此预算从 B2 中替换，不另加到 30 小时之外。两小时仍不能建立双向正确微基准则停止。

最小原型保留候选聚合和 RX/L2 算法，只更换批次传输/通知。用含 header+count+payload 的连续消息、独立 uint64 signal 和 generation，不能把一个 signal 当作先前无关写入的自动屏障。signal 表示交付，不是 L2 提交或全局完成；ACK 仍在有效更新入队之后，槽位等待其 ACK 才复用。[N1]

先试有本地源缓冲完成保证的 put-with-signal；采用 nbi 时必须按实际版本文档保证 source 生命周期，不能 API 返回就覆盖 retained journal。当前共享内存 SoA 到连续消息的打包成本计入对照；共享内存 source 是否可用按安装版本和 P2P 支持核实。[N1]

优先比较适配真实批量的 warp/block 调用，不能拿单线程库复制与原 warp 复制不对等比较。在相同安全同步语义、相同批量分布、双向并发和 ACK 背压下测量。manager 保留非阻塞进展，不用等待信号把接收/终止职责锁死。两 PE 改造后的计时仍覆盖整次双卡查询，不能将 host launch/barrier 开销移出原 solve 边界。[N2][N3]

决策估算（固定工作量/调度的近似，不是预测）：`S_new = S_old / (1-f+f/r)`，f 是不可被计算覆盖的可优化部分，r 是该部分局部加速。USA 从 1.090 到 1.2，若 r=2，需 f 约 18.3%；仅有带宽提升不够。若通信时机改变工作量，另报告这种影响。不能把累加的 warp 周期当 f。

NVSHMEM 可用于明确传输的同步语义，但不会自动修复本地 candidate/mark、L2 发布和全局终止的全部同步合同。不要以删掉安全机制的旧版作为“正确实现更快”的最终依据。

## 5. 30 小时执行表与止损点

| 执行时间 | 工作 | 到点必须产出/决定 |
|---|---|---|
| 0-3 h | 固定 HEAD/同步版本；导出 38082 原始证据；短名单数据准入；分配内检查 GPU/拓扑 | dataset_manifest、version_manifest；禁止沿用错误旧数；确认 NVSHMEM 是否有条件做 |
| 3-7 h | 最多六个输入的配对基线与轻诊断；共同参数小范围校准 | 选最多两个主攻输入，保留所有淘汰理由；不扫描全数据池 |
| 7-13 h | 路线 A：退避/blocks，再小范围窗口；低成本候选先淘汰 | 冻结最多两个性能候选；没有净收益立即回退 |
| 13-21 h | B1 布局/邻近 cut；有证据再选 B2 一个结构改动；C 最多占用其中 4 h | 明确 layout、runtime 或 transport 的独立贡献；不做全参数笛卡尔积 |
| 21-24 h | 合并最小有效改动、核对同步/容量/进度；完整候选和传输定向测试 | 冻结最终源码、编译宏、输入/源点和参数；停止增加功能 |
| 24-30 h | 新的正式配对样本、独立确认、固定附加源点、八图正确性回归 | 每个目标达标/未达标结论及完整可追溯证据；无时间则标“未完成验证” |

CPU 编译/统计可以并行；同一对 A100 的性能作业必须串行独占，不让多个 agent 同时跑 GPU。探索时每候选一次预热+三次正式只供淘汰；正式结果必须另采，不能重用挑中的开发样本。

诊断优先复用 `l3_wait_diag.cuh`、`l3_progress.cuh`、`l3_diagnostics.cuh` 和 `L3_WORK_DIAG`。区分未拿到任务但仍有 outstanding 的空读与真正空闲；区分提交尝试与成功发布；不要把 last_commit 后的全部时间算成纯终止。跨 GPU clock64 不直接相减。热路径诊断会改变占用和性能，单独构建，检查寄存器、shared memory、occupancy 和 launch 错误。

## 6. 正式验收：1.2x 不是通过改分母得到的

- 对每个目标，同一分配、同一图/布局/权重/源点，对独立单卡和双卡两轮反序交错，每进程一预热+五正式，以十个正式样本的中位数计算 S。GPU 数必须真实为 1/2，计时结束必须等两卡查询全部完成。
- 冻结配置后，在独立的新一轮重复运行确认 S >= 1.20；报告 IQR/MAD、全部原样样本和每轮结果。阈值附近不稳定就增加有限确认或标为未确认，不挑最小值、不提前停止只保留幸运样本。
- 单卡允许独立选择合理本地参数，不能用双卡程序的 -n 1 代替无 L3 单卡。共享算法优化同步给单卡。既报告同布局 S，也检查“已试合理布局中最佳单卡 / 新双卡”，防止靠重排拖慢单卡制造优势。
- 图结构可以按设备数使用不同物理表示，但规范化逻辑图、方向、权重和源点必须一致；重排的规范 CSR 给双方；增强边同给双方。oracle 只做计时外校验，不做 runtime seed。
- 全部结果逐顶点对照原始逻辑图的 int64 oracle，保留不可达语义；新增图通过容量检查。最终候选复用 candidate 交错、满槽重试、ACK 延迟、RX 提交边界、L2 发布、换源及最终完成测试。
- 最终报告注明同步实现和数值范围；仅快版通过输出不等于安全实现完成。未核实的 ordering、容量或进度问题不能由“通过很多次”替代，也不能用其快数宣布正式验收通过。

探索结果分为：已确认 >=1.20；有改善但 <1.20；仅某固定源点达标；仅微基准/筛选达标；失败或未完成。缺测记 null，不能记 0；不要承诺必达，也不因 30 小时用完就修改验收阈值。

## 7. 交付、推送与论文回填

新增性能工作在独立分支进行，不覆盖旧 evidence。建议以下名称是待创建产物，不是声称已有接口：

- `scripts/multigpu/run_l3_30h.py`：显式图/参数入口，复用现有 run_one、计时和 oracle；记录实际生效设置。
- `L3_30h_performance_report.md`：全部候选、失败、每图每源 T1/T2、最终版本、同步边界和结论。
- 新目录中的 dataset/version manifests、records、raw logs、correctness、capacity 和复现命令；大图/二进制不提交，只提交必要清单和小型证据。
- `evidence/l3_latest_rerun_38082/`：仅在定位并核查原始文件后建立，导出最新八图数据、汇总与绘图源。后续最终 30h 结果另建目录，不能覆盖旧批次。

论文回填只使用实际完成结果：更新一张既有单栏 L3 图和几句结果，不增加完整系统章节。数据集清单不是新增实验结果，1.2x 是目标不是当前结论。当前稿件必须把 37327/37646 的六配置图、38059 的同步版结果和 38082 的回退版复测分开；缺少最新逐图原始时间时，不用 GM 或 USA 数字反推其他图。

## 8. 来源

仓库引用均以本任务开头的提交为准：

- [A100 数据清单](A100_integer_dataset_inventory.md)：统计方法、候选文件和排除项。
- [最新修订报告](L3_minimal_revision_report.md)：38059/38075 与文末 38082 的区别、同步边界。
- [六配置结果](L3_adds_samegraph_sixconfig_results.md)；[补充检查](L3_supplementary_experiments_results.md)。
- `SSSP/main.cu`、`SSSP/csr_graph.cu`、`SSSP/graph_partition.h`：输入、单进程双 GPU 和计时。
- `SSSP/l3/l3_boundary_index.cuh`、`l3_window.h`、`l3_bulk.cuh`、`l3_receive.cuh`、`l3_transport.cuh`、`SSSP/sssp_run.cu`：源码落点。
- `graph_transform/reorder_gr.cu`、`scripts/multigpu/build_l3_paper_config.sh`、`run_supplement_matrix.py`：已有重排、构建与运行接口。
- [N1 NVIDIA Signaling Operations](https://docs.nvidia.com/nvshmem/api/latest/gen/api/signal.html)：signal 的对应数据保证、源缓冲完成和 nbi。
- [N2 NVIDIA Using NVSHMEM](https://docs.nvidia.com/nvshmem/api/latest/using.html)：PE/GPU、对称内存和初始化约束。
- [N3 NVIDIA Device APIs](https://docs.nvidia.com/nvshmem/release-notes-install-guide/best-practice-guide/apis.html)：P2P 使用 SM、warp/block 选择与成本。

外部文档核查日期：2026-09-23。部署时以实际安装版本的文档为准。优化优先级、短名单和时间分配为本任务的工程判断，不是数据清单或代码已经证明的收益。
