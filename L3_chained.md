
“L3_CHAIN_PARTITION 运行时链处理”的限时验证。

这是增量任务，不是替换原计划。请继续保留原计划的目标、基线、
实验记录和已取得的优化成果。本次计划是想尝试将shortcut的收益化为L3私有化，即在原图G上实现双卡快于单卡，所有收益归因于L3算法本身而不是归因于预处理。

一、执行顺序与预算

不要直接终止正在运行的实验，也不要覆盖当前未提交的代码修改。
先记录当前代码版本、工作区差异、实验进度及剩余时间。

可以立即开展不占用 GPU 的代码核查和已有记录检索；
GPU 实验等当前任务进入安全切换点后再安排。
正式性能测量不得与其他占用同一 GPU 的实验并行运行。

在独立分支、worktree 或隔离的源码副本中实施，确保能够回退，
不破坏原计划已经验证有效的配置。

二、先核查已有原型，不要重新实现静态 shortcuts

优先检查以下代码，以及仓库中对应的历史实验、日志和失败记录：

SSSP/l3/l3_chain_partition.h
SSSP/l3/l3_chain_partition.cuh
SSSP/l3/l3_receive.cuh
SSSP/sssp_run.cu
SSSP/main.cu

本次验证的对象是：
在原始 CSR 上，利用链索引和运行时距离快照，
对远端更新及其派生的本地激活进行链展开，
减少链内部顶点的逐个调度和重复入队。

本次不是：
构造增强图 G+ 后只让双卡使用，
或者仅通过“只在双卡开启”来声称 chain 收益属于 L3。

请以当前实际代码为准，确认：
RX 来源标记如何传播、什么条件下真正触发链展开、
内部顶点何时抑制入队、端点如何继续传播，
以及 last_processed 和终止检测如何覆盖这些工作。

重点核对该原型与 L3_TERM_ONLY_WORKER、L3_DIRECT_RX、
L3_CHAIN_SHORTCUTS 等开关及其他优化的兼容性。
若历史记录已经说明该路线失败，先判断失败原因是否仍然存在，
不要不加分析地重复相同实验。

优先复用现有实现，只做完成验证所需的最小修改。
若发现必须大规模重写协议或调度框架，记录原因并停止扩张范围。

三、建立最小且可归因的实验对照

先测试 USA，再根据链覆盖率和链长度统计预先选定一张道路图。
不要根据速度结果事后挑选第二张图。

使用同一份原始图 G、相同布局、相同逻辑源点及原计划的验证口径，
至少比较：

A：单卡 MLMQ 和 单卡ADDS
B：双卡 L3，关闭运行时 chain partition。
C：双卡 L3，开启运行时 chain partition。

A 使用合理的单卡配置，不得人为削弱单卡基线。
B 和 C 除 chain 及其必要索引外，应保持其他配置一致。
尤其不能同时切换 term-only worker、终止机制、线程配置等，
再把全部时间差归因于 chain。

如果为了原型兼容而改变了公共执行路径，
请额外保留原计划当前最佳双卡配置作为参考。

确认输入文件确实是原始 G：
仅关闭 L3_CHAIN_SHORTCUTS 宏，并不能排除输入本身已经是 G+。

已有单卡 G+ 和双卡 G+ 结果可作为竞争力参考，但必须单独标注，
不能与原始 G 的结果混用，声称是同图对照或 chain 的净收益。

四、先确认正确性和实际命中，再判断性能

先做小规模定向检查，再跑正式图的逐顶点距离验证。
检查重点包括：双向链、两个方向权重不同、零权边、
链端点和内部顶点收到进一步改进、并发改进、
权重累加溢出，以及不满足严格链条件时的回退。

特别确认：
内部顶点不再入队时，其必要传播已被链展开覆盖，
且未完成的链处理仍被正确计入终止判断。
不能通过遗漏工作获得“加速”。

优先复用兼容的轻量诊断，至少确认：
链路径实际触发多少次、减少了多少内部激活，
以及是否出现大量重复整链展开。
诊断配置与正式计时配置分开，正式计时关闭额外插桩，
不要为了计数启用原型不兼容的宏。

正式结果采用原计划相同的重复次数、源点集合和统计方法。
计时必须覆盖完整求解及必要的距离物化工作。
索引构建、上传和额外显存单独报告，保持清楚一致的计时边界。

分别报告两个比值：

运行时 chain 的双卡净收益 = T(B) / T(C)
同图双卡加速比             = T(A) / T(C)

核心目标仍然是稳定达到同图双卡加速比 >= 1.20，
不是仅证明入队数减少或某一次运行更快。

若 C 表现出明显潜力，且剩余预算允许，再增加小规模单卡控制：
让单卡也能合理使用相同链索引处理本地激活，
用于区分通用链处理收益与远端更新感知策略的额外收益。

不能把“单卡没有 RX 标记，所以从不触发”当作有效的单卡链对照。
若这个控制超出预算，明确标记归因尚未完成，不作独占收益结论。

五、决策与交付

若运行时 chain 能稳定降低完整求解时间，并具备达到目标的潜力，
将其提升为后续优化候选；与其他优化组合前，先保留独立消融结果。

若链命中很少、重复展开抵消收益、正确性修复超出预算，
或者始终落后于原计划当前最佳方案，
则停止该分支，保留记录，回到原计划。

不要为了让 chain 看起来“属于 L3”而改变比较口径；
也不要为了追求 1.20 而只保留有利源点、单次结果或失败之外的样本。

输出 L3_chain_runtime_trial.md，包含：
代码版本及差异、输入图标识、源点、完整编译与运行命令、
正确性结果、原始日志位置、时间统计、诊断指标、
索引构建与上传成本，以及继续或停止的明确结论。

先简短反馈当前任务进度、可安全插入该尝试的时间点、
剩余预算和准备复用的代码路径，然后按上述范围执行；
除非遇到需要终止现有任务、破坏性修改或超出预算的情况，
不必等待我再次确认。
